# Minimal dependencies for FastAPI inference on Railway (CPU).
# Install: pip install -r requirements-prod.txt
# tensorflow-cpu avoids GPU stacks and reduces install + runtime footprint vs full tensorflow.

fastapi==0.121.3
uvicorn[standard]==0.38.0
python-multipart==0.0.22
tensorflow-cpu==2.21.0
numpy==2.2.6
pillow==11.3.0
h5py==3.14.0
pydantic==2.12.4
